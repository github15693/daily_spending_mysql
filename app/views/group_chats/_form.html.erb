<%= form_for(@group_chat) do |f| %>
  <% if @group_chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_chat.errors.count, "error") %> prohibited this group_chat from being saved:</h2>

      <ul>
      <% @group_chat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
