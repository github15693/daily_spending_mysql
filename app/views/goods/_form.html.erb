<%= form_for(@good, remote:true) do |f| %>
    <% if @good.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@good.errors.count, "error") %> prohibited this good from being saved:</h2>
          <ul>
            <% @good.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group form-inline text-center">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>
      <%= f.label :price, class:'margin-left-20' %>
      <%= f.text_field :price, class: 'form-control' %>
      <%= f.submit class: 'btn btn-success margin-left-20' %>
    </div>
<% end %>
