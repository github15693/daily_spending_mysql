<%= form_for(@individual_chat) do |f| %>
  <% if @individual_chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@individual_chat.errors.count, "error") %> prohibited this individual_chat from being saved:</h2>

      <ul>
      <% @individual_chat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sending_user %><br>
    <%= f.number_field :sending_user %>
  </div>
  <div class="field">
    <%= f.label :receiving_user %><br>
    <%= f.number_field :receiving_user %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
